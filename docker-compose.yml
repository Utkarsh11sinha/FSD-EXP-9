services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    image: react-docker-multistage:latest
    ports:
      - "8080:80"
    environment:
      API_BASE_URL: ${API_BASE_URL:-/api}
      APP_ENV: ${APP_ENV:-production}
