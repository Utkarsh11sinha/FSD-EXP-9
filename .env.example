# Build-time variable used by Vite during docker build
VITE_API_BASE_URL=https://api.example.com

# Runtime variables injected by nginx entrypoint script
API_BASE_URL=https://api.example.com
APP_ENV=production
